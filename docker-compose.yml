version: '2'

volumes:
  settings:

services:
  kiosk:
    restart: always
    build: ./kiosk
    privileged: true
    ports:
      - 5011:5011
    volumes:
      - 'settings:/data'
  scheduler:
    restart: always
    build: ./scheduler
    privileged: true
  wifi-connect:
    build: ./wifi-connect
    restart: always
    network_mode: host
    privileged: true
    labels:
      io.balena.features.dbus: '1'
      io.balena.features.firmware: '1'
  fbcp:
    build: ./fbcp
    restart: on-failure
    privileged: true
  browsercontrol:
    build: ./browsercontrol
    restart: always
    privileged: true
    ports:
      - 8080:80
  pulse:
    # https://github.com/balenablocks/pulse
    image: balenablocks/pulse
    restart: always
    privileged: true
    labels:
      io.balena.features.supervisor-api: '1'
    environment:
      GPIO_PIN: 4
      ALWAYS_USE_HTTPSERVER: True
    ports:
      - 7575:7575
  noisy:
    depends_on:
        - pulse
    # https://hub.docker.com/_/nginx
    build: ./noisy
    restart: always
    ports:
      - 80:80
      - 8585:8585
